#include <stdio.h>

int main() {
    int age = 12; 
    double gift = 10.0; 
    double total = 0.0; 

    while (gift <= 1000.0) {
        total += gift; 
        gift *= 2.0; 
        age++; 
    }
    printf("%d\n", age);
    printf("%.2f\n", total); 
}