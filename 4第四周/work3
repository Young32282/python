#include <stdio.h>
#include<math.h>
int main(){
    double a,b,c,area;
    scanf("%f %f %f",&a,&b,&c);
    double s=(a+b+c)/2;
    area=sqrt(s*(s-a)*(s-b)*(s-c))
    if (s(s-a)(s-b)(s-c)<0){
        printf("The input side lengths are not correct.");
    } else {
        printf("%f.2/n",s2)
    }
return 0
    }