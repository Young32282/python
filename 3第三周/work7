#include <stdio.h>  

int main() {  

    float x1, y1, x2, y2;
    
    scanf("%f,%f,%f,%f", &x1, &y1, &x2, &y2);  
    

    float midpointX = (x1 + x2) / 2.0;  
    float midpointY = (y1 + y2) / 2.0;  
    

    printf("(%.2f, %.2f)\n", midpointX, midpointY);  
    return 0;
}