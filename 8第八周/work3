#include <stdio.h>
double poly(double a,double b,double c,double x){
    double y = a*x*x+b*x+c;
    return y;
}
int main (){
    double  a,b,c,x;
    scanf("%f %f %f %f",&a,&b,&c,&x);
    printf ("%.6f",poly(a,b,c,x));
    return 0;
}