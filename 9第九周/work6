#include<stdio.h>
long fact(int n){
    long f;
    if(n==1){
        f=1;
    }
    else{
        f=n*fact(n-1);
    }
    return(f);
}
int main(){
    long y;
    int m;
    scanf("%D",&m);
    y=fact(m);
    printf("%d",y);
    return 0;
}