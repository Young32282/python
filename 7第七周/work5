#include<stdio.h>
#include<math.h>
int main(){
    double y = 0;
    for(float x =1;x<3.0;x+=0.1){
        y=1 + x + 0.5 * x * x + (1.0 / 6) * x * x * x + (1.0 / 24) * x * x * x * x;;
        printf("%.1f,%.4f\n",x,y);

    }
return 0;
}