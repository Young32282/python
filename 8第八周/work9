#include <stdio.h>
int combination(int n, int m) {
    int result = 1;
    for (int i = 1; i <= m; ++i) {
        result *= n - i + 1; 
        result /= i;         
    }
    return result;
}
int main(){
   int n,m;
   scanf("%d %d",&n,&m);
   printf("%d",combination(n,m));
   return 0;
}